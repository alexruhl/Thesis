<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexruhl.bachelorthesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Questions5Activity" />
        <activity android:name=".Questions4Activity" />
        <activity android:name=".Questions3Activity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".MorningActivity" />
        <activity android:name=".TrainingActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".Questions2Activity" />
        <activity android:name=".Questions1Activity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <provider
            android:authorities="com.example.alexruhl.bachelorthesis"
            android:name="androidx.core.content.FileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>
    </application>

</manifest>